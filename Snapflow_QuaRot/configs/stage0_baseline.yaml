name: stage0_baseline
# local path confirmed available at /data/jameskimh/james_lebero_pretrained/pi05_libero_finetuned
pretrained_path: /data/jameskimh/james_lebero_pretrained/pi05_libero_finetuned
calib_stats_path: artifacts/stage0_calib_stats.pt
output_dir: results/stage0

policy:
  n_action_steps: 10
  num_inference_steps: 10

eval:
  task: libero_10
  # 10 tasks × 10 episodes = 100 total
  n_episodes: 10
  batch_size: 10
  device: cuda
  use_amp: false
  start_seed: 1000
